{
  "name": "install-lib",
  "version": "1.0.6",
  "description": "Install a module and its dependencies as a single module",
  "repository": "git@github.com:daleblackwood/install-lib.git",
  "main": "./index.js",
  "bin": "./index.js",
  "scripts": {
    "build": "node ./",
    "start": "node ./",
    "install-lib": "node ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dale Williams",
  "license": "MIT",
  "dependencies": {
    "argparse": "^1.0.10",
    "fs-extra": "^8.1.0"
  }
}
